role: tx
project: usrp-csi-tx

device_args: ""
channels: [0]

freq: 5890000000
samp_rate: 10000000
lo_offset: 0
tx_gain: 0.75
encoding: 0
pdu_length: 500
interval_ms: 300

capture:
  save_metadata: true
  metadata_dir: "logs"

dataset_stage: "siso_tx_baseline"
notes: "Tx-only USRP B210 WiFi/CSI sensing baseline"
